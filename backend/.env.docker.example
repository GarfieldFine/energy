# Docker 一键部署时：复制本文件为 .env（与 backend 目录下 .env 合并或覆盖），
# 并将能耗 CSV、元数据、字典、司空 jsonl、PDF 知识库、kb_index.sqlite 放到 backend/data/ 对应子路径。
# 最小可运行：至少准备 building_energy_hourly.csv 与 metadata_subset.csv 放入 data/imported/

ENERGY_CSV=/app/data/imported/building_energy_hourly.csv
METADATA_CSV=/app/data/imported/metadata_subset.csv
DATA_DICTIONARY_CSV=/app/data/imported/data_dictionary.csv
KB_ROOT=/app/data/kb_documents
KB_INDEX_DB=/app/data/kb_index.sqlite
SIKONG_JSONL=/app/data/sikong/sikong_sft_all.jsonl

# 可选：Chatchat、LLM（不填则仅用本机 RAG）
# CHATCHAT_BASE_URL=http://host.docker.internal:7861
# LLM_API_BASE=
